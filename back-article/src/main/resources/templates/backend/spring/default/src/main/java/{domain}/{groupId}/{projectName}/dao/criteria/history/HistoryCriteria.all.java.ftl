package  ${config.domain}.${config.groupId}.${config.projectName}.${config.dao}.criteria.history<#if pojo.subModule.folder??>.${pojo.subModule.folder}</#if>;

import ${config.domain}.${config.groupId}.${config.projectName}.zynerator.history.HistCriteria;


public class ${pojo.name}HistoryCriteria extends HistCriteria {

}
